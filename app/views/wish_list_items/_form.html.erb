<%= form_for(@wish_list_item) do |f| %>
  <% if @wish_list_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@wish_list_item.errors.count, "error") %> prohibited this wish_list_item from being saved:</h2>

      <ul>
      <% @wish_list_item.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :category_id %><br />
    <%= f.text_field :category_id %>
  </div>
  <div class="field">
    <%= f.label :product_id %><br />
    <%= f.text_field :product_id %>
  </div>
  <div class="field">
    <%= f.label :timestamps %><br />
    <%= f.text_field :timestamps %>
  </div>
  <div class="field">
    <%= f.label :available %><br />
    <%= f.check_box :available %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
